/*var ctrl = new ScrollMagic.Controller();

// Create scenes in jQuery each() loop
$(".fadein").each(function (i) {
    var fadein = $(this);
    var tl = new TimelineMax();

    tl.from(fadein, 0.25, { autoAlpha: 0 });

    new ScrollMagic.Scene({
        triggerElement: this,
        triggerHook: 1
    })
        .setTween(tl)
        .duration("85%")
        .addTo(ctrl);

});*/